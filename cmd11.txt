The new Blob run-from-package deployment is now successfully doing:

1. Function ZIP validation - SUCCESS
2. Blob package upload - SUCCESS
3. WEBSITE_RUN_FROM_PACKAGE update - SUCCESS
4. Function App restart - SUCCESS

The only remaining failure is:

Synchronizing Function triggers...
BadRequest:
Encountered an error (ServiceUnavailable) from host runtime.

The workflow currently calls syncfunctiontriggers immediately after
`az functionapp restart`.

Modify ONLY the post-restart readiness/sync logic.

Do NOT change:
- Terraform
- RBAC
- storage roles
- private endpoints
- DNS
- WEBSITE_RUN_FROM_PACKAGE implementation
- package upload
- Function managed identity
- public network settings

After:

az functionapp restart \
  --name "$FUNCTION_APP" \
  --resource-group "$RESOURCE_GROUP"

add a retry/wait loop before declaring deployment failure.

Retry trigger synchronization up to 12 times with 10 seconds between
attempts.

Use:

SYNC_SUCCESS=false

for attempt in {1..12}; do
  echo "Synchronizing Function triggers - attempt ${attempt}/12..."

  if az rest \
    --method post \
    --url "https://management.azure.com/subscriptions/${ARM_SUBSCRIPTION_ID}/resourceGroups/${RESOURCE_GROUP}/providers/Microsoft.Web/sites/${FUNCTION_APP}/syncfunctiontriggers?api-version=2022-03-01" \
    --output none; then

    echo "Function trigger synchronization succeeded."
    SYNC_SUCCESS=true
    break
  fi

  echo "Function host is not ready yet. Waiting 10 seconds..."
  sleep 10
done

if [ "$SYNC_SUCCESS" != "true" ]; then
  echo "ERROR: Function host did not become ready after 12 attempts."
  exit 1
fi

After successful trigger synchronization, keep the existing
WEBSITE_RUN_FROM_PACKAGE validation.

Important:
- Do not use `|| true` to hide the final failure.
- Do not remove the package.
- Do not introduce SAS/storage keys/secrets.
- Do not add any new diagnostic infrastructure.
- Do not revert to Azure/functions-action@v1.
- Keep set -euo pipefail, but structure the az rest call inside the `if`
  so retry failures do not terminate the script.
- Show only the workflow diff.
